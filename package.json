{
  "name": "codex-autoreview-marketplace",
  "version": "0.3.1",
  "private": true,
  "description": "Single-plugin Claude Code marketplace for codex-autoreview — automatic background Codex review of plans and code changes.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "npm test --prefix plugins/codex-autoreview"
  }
}
